# Proxy Container

# Hostname for KBase User Interfaces (other than kbase-ui-py)
# KBASE_UI_HOSTNAME=ci.kbase.us

# Hostname for KBase service endpoints
KBASE_SERVICE_HOSTNAME=ci.kbase.us

# disabled
# UI_HOSTNAME=

# the ip or host of the kbase-ui container. This is required
# may need to override in the run command
KBASE_UIPY_HOSTNAME=kbase-ui-py:8000
